{
  "name": "arc-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Arc Onboard MVP",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network arcTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.5"
  },
  "dependencies": {
    "solc": "^0.8.30"
  }
}
