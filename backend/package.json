{
  "name": "arc-onboard-backend",
  "version": "1.0.0",
  "description": "Backend for ARC Onboard MVP",
  "main": "src/server.ts",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "tsc",
    "build:frontend": "cd ../frontend && npm run build",
    "start": "node dist/server.js",
    "prod": "npm run build && npm run build:frontend && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.0"
  }
}

